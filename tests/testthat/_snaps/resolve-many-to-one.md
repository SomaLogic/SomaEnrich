# `.resolve_many_to_one() produces the expected messaging`

    Code
      t <- .resolve_many_to_one(ranks = ranks, verbose = TRUE)
    Message
      > There are 782 feature(s) with multiple elements in the ranked input vector.
      v A total of 889 lower-ranked element(s) were dropped.

